#!/usr/bin/env python3
"""Plot beam progress and mutation prevalence at every available depth."""
from spurs_plotting import main

if __name__ == "__main__":
    raise SystemExit(main(default_plot="beam"))
