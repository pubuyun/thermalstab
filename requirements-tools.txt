# Utilities only; do not reinstall the existing SPURS / torch environment.
PyYAML>=6.0,<7
# The validated Python 3.12 SPURS/PyTorch environment uses NumPy 1.26.4.
# NumPy 1.x has no Python 3.13 wheels, so local plotting on 3.13 uses 2.x.
numpy>=1.24,<2; python_version < "3.13"
numpy>=2,<3; python_version >= "3.13"
matplotlib>=3.7,<4
